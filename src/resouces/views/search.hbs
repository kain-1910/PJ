<div class='search-form'>
    {{! Form default behavior
        method: tùy chỉnh method khi form submit - mặc định là GET
        action: tùy chỉnh route để thực hiện method khi submit - mặc định là trang hiện tại
        khi submit thì sẽ lấy name của input làm query param (GET)
        khi là POST thì khi submit sẽ không truyền query param lên URL
     }}
    <form method='GET' action=''>
        <div class='form-group'>
            <label for='search-input'>Search Infomation</label>
            <input
                type='text'
                class='form-control'
                name='q'
                id='search-input'
                aria-describedby='emailHelp'
                placeholder='Enter key'
            />
        </div>
        <div class='form-group'>
            <label for='gender-input'>Select gender</label>
            <select class='form-control' name='gender' id='gender-input' aria-describedby='emailHelp'>
                <option value=''>Select gender</option>
                <option value='male'>Nam</option>
                <option value='female'>Nữ</option>
            </select>
        </div>
        <button type='submit' class='btn btn-primary'>Submit</button>
    </form>
</div>